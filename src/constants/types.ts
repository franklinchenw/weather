export type UNIT_TYPE = "metric" | "imperial";
export type TEMP_SIGN = "°C" | "°F";

export const UNIT_TYPES = {
  METRIC: "metric",
  IMPERIAL: "imperial",
};

export const TEMP_SIGNS = {
  METRIC: "°C",
  IMPERIAL: "°F",
};
